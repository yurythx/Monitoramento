# Configurações de Ambiente - Stack Monitoramento
# Ubuntu 24.04 + aaPanel
# Substitua os valores conforme sua configuração

# ===========================================
# CONFIGURAÇÕES POSTGRESQL (ZABBIX)
# ===========================================
POSTGRES_USER=zabbix
POSTGRES_PASSWORD=ZabbixDB2024!Secure
POSTGRES_DB=zabbix

# ===========================================
# CONFIGURAÇÕES ZABBIX SERVER
# ===========================================
DB_SERVER_HOST=postgres
DB_SERVER_PORT=5432
ZBX_SERVER_HOST=zabbix-server
ZBX_SERVER_PORT=10051

# ===========================================
# CONFIGURAÇÕES MYSQL (GLPI)
# ===========================================
MYSQL_ROOT_PASSWORD=MySQLRoot2024!Secure
MYSQL_DATABASE=glpi
MYSQL_USER=glpi
MYSQL_PASSWORD=GLPIDB2024!Secure

# ===========================================
# CONFIGURAÇÕES GLPI
# ===========================================
GLPI_DB_HOST=mysql
GLPI_DB_PORT=3306
GLPI_DB_NAME=glpi
GLPI_DB_USER=glpi
GLPI_DB_PASSWORD=GLPIDB2024!Secure

# ===========================================
# CONFIGURAÇÕES GRAFANA
# ===========================================
# Estas configurações estão no arquivo Grafana/.env
# GF_SECURITY_ADMIN_USER=admin
# GF_SECURITY_ADMIN_PASSWORD=GrafanaAdmin2024!Secure

# ===========================================
# CONFIGURAÇÕES DE REDE
# ===========================================
# IP do servidor Ubuntu (ajustar conforme necessário)
HOST_IP=192.168.0.121

# ===========================================
# CONFIGURAÇÕES DE TIMEZONE
# ===========================================
TZ=America/Sao_Paulo

# ===========================================
# CONFIGURAÇÕES DE VOLUMES
# ===========================================
# Os volumes serão criados automaticamente pelo Docker
# Localização padrão: /var/lib/docker/volumes/

# ===========================================
# INSTRUÇÕES DE USO
# ===========================================
# 1. Copie este arquivo para .env na raiz do projeto Monitoramento
# 2. Ajuste as senhas e o HOST_IP conforme sua configuração
# 3. Configure os domínios no aaPanel:
#    - zabbix.seudominio.com → 192.168.0.121:8080
#    - glpi.seudominio.com → 192.168.0.121:8081
#    - grafana.seudominio.com → 192.168.0.121:3000
#    - portainer.seudominio.com → 192.168.0.121:9000
# 4. Configure SSL para todos os domínios
# 5. Execute: docker-compose up -d

# ===========================================
# CONFIGURAÇÕES FINAIS NECESSÁRIAS
# ===========================================
# Após o deploy, configure:
# 1. Zabbix: Complete wizard de instalação
# 2. GLPI: Complete wizard de instalação  
# 3. Grafana: Configure data sources
# 4. Portainer: Crie conta de administrador